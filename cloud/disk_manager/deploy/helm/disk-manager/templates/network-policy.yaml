apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: disk-manager-policy-{{ .Values.serviceKind }}
spec:
  podSelector:
    matchLabels:
      app: {{ include "common.names.fullname" . }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - {}
  egress:
    - {}