AsyncDestroyHandleEnabled: true
AsyncCreateHandleEnabled: true
# Slow the queue drain so restarts can happen while confirmations are still
# pending in the queue. Process one operation per interval regardless of the
# default batch size.
AsyncHandleOperationBatchSize: 1
AsyncHandleOperationDrainPeriod: 50
