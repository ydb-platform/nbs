#pragma once

/*
 * SPDK normally generates this header during its own build.  The FileStore
 * fsdev plugin only needs public fsdev/thread declarations, so empty defaults
 * are sufficient for YA builds that consume the mirrored SPDK headers.
 */
