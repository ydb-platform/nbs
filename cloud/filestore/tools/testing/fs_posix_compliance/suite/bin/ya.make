PACKAGE()

IF(ARCH_X86_64)
    FROM_SANDBOX(92513045000 EXECUTABLE OUT flock)
ELSEIF (ARCH_ARM64)
    FROM_SANDBOX(92513045001 EXECUTABLE OUT flock)
ELSE()
    MESSAGE(FATAL_ERROR "Unsupported platform")
ENDIF()

END()
