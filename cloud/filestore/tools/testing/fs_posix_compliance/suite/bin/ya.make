PACKAGE()

IF(ARCH_X86_64)
    FROM_SANDBOX(13128064589 EXECUTABLE OUT flock)
ELSEIF (ARCH_ARM64)
    FROM_SANDBOX(13128063138 EXECUTABLE OUT flock)
ELSE()
    MESSAGE(FATAL_ERROR "Unsupported platform")
ENDIF()

END()
