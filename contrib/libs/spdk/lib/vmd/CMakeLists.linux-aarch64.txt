
# This file was generated by the build system used internally in the Yandex monorepo.
# Only simple modifications are allowed (adding source-files to targets, adding simple properties
# like target_include_directories). These modifications will be ported to original
# ya.make files by maintainers. Any complex modifications which can't be ported back to the
# original buildsystem will not be accepted.



add_library(spdk-lib-vmd)
target_compile_options(spdk-lib-vmd PRIVATE
  $<IF:$<CXX_COMPILER_ID:MSVC>,,-Wno-everything>
)
target_include_directories(spdk-lib-vmd PRIVATE
  ${CMAKE_SOURCE_DIR}/contrib/libs/spdk/include
)
target_link_libraries(spdk-lib-vmd PUBLIC
  contrib-libs-linux-headers
  spdk-lib-log
)
target_sources(spdk-lib-vmd PRIVATE
  ${CMAKE_SOURCE_DIR}/contrib/libs/spdk/lib/vmd/led.c
)

add_global_library_for(spdk-lib-vmd.global spdk-lib-vmd)
target_compile_options(spdk-lib-vmd.global PRIVATE
  $<IF:$<CXX_COMPILER_ID:MSVC>,,-Wno-everything>
)
target_include_directories(spdk-lib-vmd.global PRIVATE
  ${CMAKE_SOURCE_DIR}/contrib/libs/spdk/include
)
target_link_libraries(spdk-lib-vmd.global PUBLIC
  contrib-libs-linux-headers
  spdk-lib-log
)
target_sources(spdk-lib-vmd.global PRIVATE
  ${CMAKE_SOURCE_DIR}/contrib/libs/spdk/lib/vmd/vmd.c
)
