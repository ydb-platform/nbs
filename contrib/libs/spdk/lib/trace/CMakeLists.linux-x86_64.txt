
# This file was generated by the build system used internally in the Yandex monorepo.
# Only simple modifications are allowed (adding source-files to targets, adding simple properties
# like target_include_directories). These modifications will be ported to original
# ya.make files by maintainers. Any complex modifications which can't be ported back to the
# original buildsystem will not be accepted.



add_library(spdk-lib-trace)
target_compile_options(spdk-lib-trace PRIVATE
  $<IF:$<CXX_COMPILER_ID:MSVC>,,-Wno-everything>
)
target_include_directories(spdk-lib-trace PRIVATE
  ${CMAKE_SOURCE_DIR}/contrib/libs/spdk/include
)
target_link_libraries(spdk-lib-trace PUBLIC
  contrib-libs-linux-headers
  spdk-lib-json
  spdk-lib-jsonrpc
  spdk-lib-log
  spdk-lib-rpc
  spdk-lib-util
)
target_sources(spdk-lib-trace PRIVATE
  ${CMAKE_SOURCE_DIR}/contrib/libs/spdk/lib/trace/trace.c
)

add_global_library_for(spdk-lib-trace.global spdk-lib-trace)
target_compile_options(spdk-lib-trace.global PRIVATE
  $<IF:$<CXX_COMPILER_ID:MSVC>,,-Wno-everything>
)
target_include_directories(spdk-lib-trace.global PRIVATE
  ${CMAKE_SOURCE_DIR}/contrib/libs/spdk/include
)
target_link_libraries(spdk-lib-trace.global PUBLIC
  contrib-libs-linux-headers
  spdk-lib-json
  spdk-lib-jsonrpc
  spdk-lib-log
  spdk-lib-rpc
  spdk-lib-util
)
target_sources(spdk-lib-trace.global PRIVATE
  ${CMAKE_SOURCE_DIR}/contrib/libs/spdk/lib/trace/trace_flags.c
  ${CMAKE_SOURCE_DIR}/contrib/libs/spdk/lib/trace/trace_rpc.c
)
