
# This file was generated by the build system used internally in the Yandex monorepo.
# Only simple modifications are allowed (adding source-files to targets, adding simple properties
# like target_include_directories). These modifications will be ported to original
# ya.make files by maintainers. Any complex modifications which can't be ported back to the
# original buildsystem will not be accepted.



add_library(event-subsystems-vmd INTERFACE)
target_link_libraries(event-subsystems-vmd INTERFACE
  spdk-lib-event
  spdk-lib-json
  spdk-lib-jsonrpc
  spdk-lib-log
  spdk-lib-rpc
  spdk-lib-thread
  spdk-lib-vmd
)

add_global_library_for(event-subsystems-vmd.global event-subsystems-vmd)
target_compile_options(event-subsystems-vmd.global PRIVATE
  $<IF:$<CXX_COMPILER_ID:MSVC>,,-Wno-everything>
)
target_include_directories(event-subsystems-vmd.global PRIVATE
  ${CMAKE_SOURCE_DIR}/contrib/libs/spdk/include
)
target_link_libraries(event-subsystems-vmd.global PUBLIC
  spdk-lib-event
  spdk-lib-json
  spdk-lib-jsonrpc
  spdk-lib-log
  spdk-lib-rpc
  spdk-lib-thread
  spdk-lib-vmd
)
target_sources(event-subsystems-vmd.global PRIVATE
  ${CMAKE_SOURCE_DIR}/contrib/libs/spdk/module/event/subsystems/vmd/vmd.c
  ${CMAKE_SOURCE_DIR}/contrib/libs/spdk/module/event/subsystems/vmd/vmd_rpc.c
)
