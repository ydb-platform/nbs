commit 473de1eff177fe8ce4a4f6264c5e53eef982c00d (HEAD -> yamaker-bison, arcadia/users/thegeorg/yamaker-bison)
author: thegeorg
date: 2024-06-16T00:26:02+03:00

    Put contrib/tools/bison under yamaker

--- contrib/tools/bison/src/files.c    (f4e9156409a06687c6a75ca842e98b674ae87b7f)
+++ contrib/tools/bison/src/files.c    (473de1eff177fe8ce4a4f6264c5e53eef982c00d)
@@ -89,6 +87,10 @@ static char *header_extension = NULL;
 | STR1, and STR2.                                                  |
 `-----------------------------------------------------------------*/
 
+#if defined _win_ || defined _WIN64 || defined _WIN32 || defined __WIN32__
+char *stpcpy(char *dst, const char *src);
+#endif
+
 static char *
 concat2 (char const *str1, char const *str2)
 {
@@ -163,16 +157,19 @@ compute_exts_from_gf (const char *ext)
   if (STREQ (ext, ".y"))
     {
       src_extension = xstrdup (language->src_extension);
+      header_extension = xstrdup(".h");
-      header_extension = xstrdup (language->header_extension);
     }
   else
     {
       src_extension = xstrdup (ext);
+      /*
       header_extension = xstrdup (ext);
       tr (src_extension, 'y', 'c');
       tr (src_extension, 'Y', 'C');
       tr (header_extension, 'y', 'h');
       tr (header_extension, 'Y', 'H');
+      */
+      header_extension = xstrdup(".h");
     }
 }
 
@@ -188,9 +178,12 @@ compute_exts_from_src (const char *ext)
      so the extenions must be computed unconditionally from the file name
      given by this option.  */
   src_extension = xstrdup (ext);
+  /*
   header_extension = xstrdup (ext);
   tr (header_extension, 'c', 'h');
   tr (header_extension, 'C', 'H');
+  */
+  header_extension = xstrdup(".h");
 }
 
 
